# =============================================================================
# Active color marker.
#
# Single source of truth for which prod color currently serves
# app.justinpyne.xyz.
#
# Updated by release-prod.yml during the cutover step.
# Read by release-prod.yml to determine the inactive color (the deploy target).
# =============================================================================

global:
  activeColor: blue
